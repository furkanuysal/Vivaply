namespace Vivaply.API.Entities.Entertainment.Igdb
{
    public enum PlayStatus
    {
        None = 0,
        PlanToPlay = 1, // Playlis
        Playing = 2,    // Playing
        Completed = 3,   // Completed
        OnHold = 4,      // On hold
        Dropped = 5      // Dropped
    }
}
